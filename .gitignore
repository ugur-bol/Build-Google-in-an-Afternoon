# Binaries
antigravity
*.exe

# Data files (regenerated at runtime)
# NOTE: `pages.jsonl` is intentionally committed for evaluation/inspection.
# The raw postings file `p.data` can be very large; it is tracked via Git LFS.
# (See `.gitattributes`.)

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
